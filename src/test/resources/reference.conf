sms {
  clickatell {
    user = "invalid"
    password = "invalid"
    api-id = "invalid"
  }
}

secret {
  persist {
    cassandra {
      contact-point {
        host = "localhost"
        host = ${?CASSANDRA_HOST}
        port = 9042
      }
    }
  }
}
